@inject IConfiguration Configuration
@model AdvertDto
@{
    string url = new Uri($"{Context.Request.Scheme}://{Context.Request.Host}/{Configuration["UserImgDir"]}/").AbsoluteUri;
}
<div class="d-flex flex-column align-items-center">
    <div class="d-flex justify-content-center gap-4 py-2 flex-wrap w-90 border rounded-1">
            @foreach (var item in ViewBag.Images)
            {
                <div class="w-30 g-2" style="height:140px;">
                <img style="object-fit: cover;" src="@(url+item.Name)" class="h-100 w-100 rounded-1">
                </div>
            }
    </div>
    <div class="d-flex flex-column m-3 gap-2 w-90">
        <div class="d-flex justify-content-between">
            <span class="fw-bold">Дата оголошення:</span>
            <span>@Model.Date.ToShortDateString()</span>
       
        </div>
        <div class="d-flex justify-content-between">
            <span class="fw-bold">Ім'я продавця:</span>
            <span>@Model.SellerName</span>
        </div>
        <div class="d-flex justify-content-between">
            <span class="fw-bold">Категорія:</span>
            <span>@Model.CategoryName</span>
        </div>
        <div class="d-flex justify-content-between">
            <span class="fw-bold">Місто:</span>
            <span>@Model.CityName</span>
        </div>
        <div class="d-flex justify-content-between">
            <span class="fw-bold">Ціна:</span>
            <span>@Model.Price гр.</span>
        </div>
        <div class="d-flex justify-content-between">
            <span class="fw-bold">Стан:</span>
            @if (@Model.IsNew)
            {
                <span>Нове </span>
            }
            else
            {
                <span>Вживане </span>
            }

        </div>
        <div class="d-flex flex-column">
            <span class="fw-bold">Заголовок:</span>
            <div class="p-2 border rounded-1">
                <span>@Model.Title</span>
            </div>
        </div>

        <div class="d-flex flex-column">
            <span class="fw-bold">Опис:</span>
            <div class="p-2 border rounded-1">
                <span>@Model.Description</span>
            </div>
        </div>
        
    </div>
</div>
